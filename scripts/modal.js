function handleModalOpen(currentModal) {
    const modal = document.getElementById(currentModal);
    modal.style.display = "flex";
  }
  
  function handleModalClose(currentModal) {
    const modal = document.getElementById(currentModal);
    modal.style.display = "none";
  }